# POS App

**Status:** Tier A — read-only catalog + demo cart (port **5177**)

```bash
npm run dev:pos-app
# or from root: npm run dev
```

Open http://localhost:5177 — loads SKUs from `GET /api/v1/retail/catalog`. Payments not connected.
